package:
  name: scimagine
  version: 0.0.1
#  version: {{ GIT_DESCRIBE_TAG }}

# build:
#   number: {{ GIT_DESCRIBE_NUMBER|int }}

requirements:
  build:
    - python
    - cython
    - numpy
    - setuptools

  run:
    - python
    - numpy

test:
    requires:
        - nose
    commands:
        - nosetests --exe sciview

about:
  home: https://github.com/scimagine/scimagine
  license: MIT
